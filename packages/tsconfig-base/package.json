{
  "name": "@ai-dos/tsconfig-base",
  "private": true,
  "version": "0.1.0",
  "description": "Shared TypeScript compiler configuration every package extends.",
  "files": [
    "base.json",
    "library.json"
  ]
}
